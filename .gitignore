# Secrets — never commit this file
secrets.json

# Lokale Cache/DB — nie pushen
.feed_cache/
*.db
*.db-shm
*.db-wal
__pycache__/
*.pyc

# Logs & generierte Ausgaben (werden vom Publish in das Repo gespiegelt,
# aber nicht als Teil des Quellcodes versioniert)
*.log
cars_legacy.json
cars.json
cars_index.json
status.json
cars/